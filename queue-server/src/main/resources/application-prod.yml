server:
  port: 8081

spring:
  # get config properties from .env file
  config:
    import: optional:file:.env[.properties]
  # redis
  data:
    redis:
      host: ${REDIS_URL}
      port: 6379

scheduler:
  enabled: true